Install nginx.
Copy htpasswd .crt, .key, .conf to conf folder.
Start nginx.
You should generate the .crt and .key youself.
The root folder is .. with the structure:

  FeatureData
    mriscan0
      aal
        feature0
        feature1
      brodmann_lr
        feature0
        feature1
    mriscan1
      aal
        feature0
        feature1
      brodmann_lr
        feature0
        feature1
